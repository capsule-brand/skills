# Capsule Skills Site — Claude Code bootstrap

Auto-generated by Clio at first onboarding. Edit freely; Clio won't overwrite.

## What this project is

Repo: `/Users/brianadducci/Developer/capsule-skills-site`. Slug: `capsule-skills-site`. Tracked by Clio (per-project session
writer), Codex (Notion → vault mirror), Aether (transcript embeddings),
Mnemosyne (fleet-state synthesis), and Cartographer (knowledge graph).

## Continuity surfaces — read on session start

The user-level `SessionStart` hook (`~/.claude/hooks/session-start.sh`)
prints these into your first message automatically. You don't need to
re-fetch them. If the hook didn't fire, query manually:

1. **Latest Clio brief for this project:**
   ```sql
   SELECT session_number, session_date, summary, commits_json, notion_block_id
   FROM clio_sessions
   WHERE project_id = (SELECT id FROM clio_projects WHERE slug = 'capsule-skills-site')
   ORDER BY session_number DESC LIMIT 1;
   ```
2. **Project memory** — auto-loaded from
   `~/.claude/projects/-Users-brianadducci-Developer-capsule-skills-site/memory/` if it exists.
3. **`~/vault/_context/now.md`** — Mnemosyne's 30-min fleet snapshot.
4. **Git state:** `git status -sb` and `git log --oneline origin/main..HEAD`.

## Conventions (inherited from the Forge fleet)

- **No bandaids.** Fix root causes. If you find a hardcoded value where a
  config should be, generalize it.
- **Memory is the vault for sessions.** When you discover a durable fact
  during a session — a decision, a gotcha, a convention — write it to
  `~/.claude/projects/-Users-brianadducci-Developer-capsule-skills-site/memory/` as
  `<category>_<slug>.md` and add a one-line entry to `MEMORY.md`.
  The user shouldn't have to ask you to "save this."
- **Direct push to main** is the default release path (per Brian's
  documented preference). PRs only when explicitly requested.
- **Pantheon naming:** `agents.id` is functional/dir-style;
  `agents.name` is the lore-bearing mythological figure.

## When you finish a session

The user-level `Stop` hook (`~/.claude/hooks/session-end.sh`) triggers
Clio's `--once` sweep so any commits made this session land in Notion in
seconds, not minutes. You don't need to do anything explicitly.

If you have durable knowledge that hasn't yet been written to memory, write
it before the session ends.
